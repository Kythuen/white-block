import gradient from 'gradient-string'
import picocolors from 'picocolors'

const { bold } = picocolors
const { retro } = gradient

export const TEXT_WELCOME_TIP = bold(retro('🚀 Welcome to Code Linter'))
export const TEXT_OPERATION_CANCEL = '❌ Cancel operation'
